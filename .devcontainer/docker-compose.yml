version: "3.8"
services:
  database:
    image: "postgres:latest"
    env_file: .env
    volumes:
      - database-volume:/var/lib/postgresql/data

  djcord:
    depends_on: [ database ]
    build: .
    stdin_open: true
    tty: true
    env_file: .env
    volumes:
        - ../:/root/djcord

volumes:
  database-volume: